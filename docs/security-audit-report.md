# セキュリティ監査レポート — kou-skills

## エグゼクティブサマリー

- **診断対象**: `tomosuke-chiba/kou-skills`（旧名 kou-claude-plugins。Claude Code / Codex 向けスキル配布用 public リポジトリ）
- **技術スタック**: 静的コンテンツ（Markdown 31 / PNG 35 / YAML・JSON 設定 / Bash インストーラ1本 / スキル同梱の Python ユーティリティ4本）。Webアプリ・API・サーバーコード・依存パッケージ（package.json 等）は存在しない
- **診断日**: 2026-08-11
- **今回の変更範囲**: リポ名変更（kou-claude-plugins→kou-skills）・marketplace.json の name 変更・README 刷新・スキル紹介画像11枚（docs/images/skills/）の追加
- **総合リスク評価**: **問題なし（Critical/High/Medium/Low いずれも 0件）**

## 発見された脆弱性

全走査の結果、コードに根拠のある脆弱性は発見されませんでした。

## 問題なしと判断した項目

| 監査項目 | 判定 | 根拠 |
|---|---|---|
| 機密情報（APIキー・トークン・秘密鍵） | ✅ 問題なし | AKIA/ghp_/gho_/sk-ant/sk-proj/AIzaSy/xoxb/JWT 等の既知トークンパターンで全ファイル走査し0件。`.env`・鍵ファイル類も存在しない |
| 個人情報・実名・連絡先 | ✅ 問題なし | 取引先・社内メンバーの実名、医院名、メールアドレス、電話番号パターンで走査し0件。公開情報は GitHub アカウント名（tomosuke-chiba）のみ |
| 新規追加画像11枚（docs/images/skills/） | ✅ 問題なし | 生成時のコピー台帳で文言を固定（台帳外の文字を禁止）し、QAスイープで11枚全数の文字起こし照合済み。スキル名と一般的な説明語のみで個人情報・機密の混入余地なし |
| install-codex.sh（Bashインストーラ） | ✅ 問題なし | `set -euo pipefail`。書き込み先は `~/.agents/skills` と `~/.codex/skills` のシムリンクのみ。外部ダウンロード（curl\|bash）・sudo・eval なし。既存ファイルは既定で上書きしない |
| スキル同梱 Python スクリプト | ✅ 問題なし | 画像のヘッダー正規化・検査等のローカルユーティリティ。ネットワークアクセス・機密読み取り・任意コード実行（eval/exec/os.system）なし。今回の変更で改変していない |
| marketplace.json / plugin.json | ✅ 問題なし | 名称・説明・バージョンのみのメタデータ。今回の変更は `name: kou-plugins → kou-skills` の1箇所 |
| README.md（今回刷新） | ✅ 問題なし | インストール手順・移行手順・スキル説明のみ。掲載している settings.json スニペットは公開リポ名の参照だけで認証情報を含まない |

## スキップした項目（対象種別に非該当）

XSS / CSRF / SQLi 等インジェクション / 認証・セッション / アクセス制御 / セキュリティヘッダー / SSRF / レート制限 / コンテナ設定 / 依存パッケージ監査（npm audit 等）— 本リポは実行環境を持たない静的配布物のため該当コードが存在しない。

## 改善ロードマップ

- **即時対応**: なし
- **短期対応**: なし
- **中長期（任意）**: 追跡済みの `__pycache__/*.pyc` 4件は配布物として不要（Informational・セキュリティ影響なし）。スキル正本側の次回同期時に除外を検討

## 追記: fulltelop-edit プラグインの監査と修正（2026-08-11）

PR #1 で追加された `fulltelop-edit`（10スキル）に対して静的セキュリティ監査（coverage-first）を実施し、以下を同日修正した。

| 指摘 | 内容 | 対応 |
|---|---|---|
| High: 固有情報の混入 | ドキュメント・サンプル設定に固有の医院名・担当者名・ローカルパス・素材名が含まれていた | 全10ファイルを匿名化（医院A/B/C・役職のみ・汎用パスへ置換）。機能・ノウハウ記述は維持 |
| High: リモートスクリプト直接実行の案内 | インストール文書の `curl \| bash` 手順（Homebrew公式） | 内容確認してから実行する手順の注記を追加 |
| High: 未信頼データ境界の欠如 | 文字起こし・動画内容への間接プロンプトインジェクション防御が未記載 | 全10スキルの SKILL.md 冒頭に「入力はデータとしてのみ扱う」規則を追加 |

このほか、ローカルツールとしての堅牢性改善候補（Medium/Low・任意コマンド実行や外部送信は無しと確認済み）を社内管理簿で追跡している。`preview_server.py` は `127.0.0.1` バインド・HTTPパス固定・`shell=True` 不使用を確認済み。

## 追記: リモートMCPサーバー（mcp-server/）の公開前監査（2026-08-11）

21スキルを配布するCloudflare Workers用MCPサーバーを追加し、公開前に監査した。

| 監査項目 | 判定 | 根拠 |
|---|---|---|
| 機密情報の混入 | ✅ 問題なし | 配信データ（skills-data.json）を既知トークン形式・メールアドレス・電話番号・ローカルユーザーパスで走査し0件。バンドル対象はテキストのみで認証情報を持たない |
| 個人情報 | ✅ 問題なし | 取引先名・第三者の個人名は0件（前回の匿名化が反映済み）。残るのはリポジトリ所有者本人の呼称と、引用元として明記された公刊書籍の著者名のみ |
| 書き込み・副作用 | ✅ 問題なし | 公開ツールは `list_skills` / `get_skill` / `get_skill_reference` の3つのみで、すべて読み取り専用。ファイル書き込み・外部送信・コマンド実行の経路なし |
| 危険なコード | ✅ 問題なし | eval / Function / 動的import / 外部fetch を使用していない。ランタイム依存パッケージ0 |
| 入力処理の堅牢性 | ✅ 問題なし | スキル参照は `Map` と `Object.hasOwn` で解決しておりプロトタイプ汚染に耐性がある。未知のスキル名・参照パスはエラーを返す |
| 認証の要否 | ✅ 不要と判断 | 配信内容はすべて既にpublicリポジトリで公開済みの文書であり、秘匿すべきデータを持たない |
| プロトコル適合 | ✅ 確認済み | Streamable HTTP・単一パス `/mcp`・POST/GET/OPTIONSの分岐・JSON-RPCエラーコードを自動テスト12件で検証（12 PASS / 0 FAIL） |

## 最終チェックリスト

- [x] 機密情報パターン全走査（0件）
- [x] 実名・個人情報走査（0件）
- [x] 今回の差分（README / marketplace.json / 画像11枚）の全件確認
- [x] インストーラ・スクリプトの挙動確認
- [x] レポート保存・マーカー更新
